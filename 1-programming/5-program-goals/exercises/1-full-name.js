'use strict';

/* Full Name

  This program combines two names into a full name.

  Data In:
    a first name
    a last name

  Data Out:
    the two names combined into one full name

*/

/* ---    --- */

let firstName = null;
while (firstName === null) {
  firstName = prompt('enter a first name');
}
console.log('first name:', firstName);

/* ---    --- */

let lastName = null;
while (lastName === null) {
  lastName = prompt('enter a last name');
}
console.log('last name:', lastName);

/* ---    --- */

// declare the variable `fullName`
// initialize it to the first and last names with a space between
__;
console.log('full name:', fullName);

/* ---    --- */

alert(fullName);
