/*
  avoid the infinite loop
*/

let count = 1
while (count < 8) {
  count = count + 2
  console.log(count);
}

while (count !== 8) { // distractor
while (count || 8) { // distractor
while (count && 8) { // distractor
