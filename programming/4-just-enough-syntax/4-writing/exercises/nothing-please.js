'use strict';

// --- gather user input ---

let input = null;

/*
  Create a while loop that checks if the input is null.

  keyword:
  check
    identifier:
    operator:
    primitive:
*/
____
  input = prompt('type nothing and click "enter"');
  console.log(message);
}

// --- create a message ---

let message = '';
if (input === '') {
  message = 'thank you for nothing!';

/*
  Write the else part of this conditional statement.

  keyword:
*/
____
  message = 'this is not nothing: ' + input;
}

/*
  Log the value stored in the message variable.

  identifier:
  identifier:
  identifier:
*/
____;

// --- display message to the user ---

alert(message);
